apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-shadow
  namespace: shadow
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: blog
      version: v2
  template:
    metadata:
      labels:
        app: blog
        version: v2
    spec:
      containers:
        - name: blog
          image: "{{ .Values.image.repository }}:v2"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: blog-svc
  namespace: shadow
spec:
  selector:
    app: blog
    version: v2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
